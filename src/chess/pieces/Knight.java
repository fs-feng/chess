package chess.pieces;

import chess.board.Square;

public class Knight extends Piece {
    public Knight(Square currentSquare, PieceColor color, String pieceName) {
        super(currentSquare, color, pieceName);
    }
}
