package chess.game;

import chess.board.Square;
import chess.pieces.PieceColor;

public class BlackPlayer extends Player{
    public BlackPlayer(PieceColor color, Square[][] chessField) {
        super(color, chessField);
    }
}
